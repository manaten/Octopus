(function() {
	socket = io.connect();
	var manager = new Octopus.__inner__.RemoteObjectManager(socket, window);
	var server = manager.getRemoteObject({ objectId:0 });
	Octopus.getServer = function() {
		return server;
	};
})();

